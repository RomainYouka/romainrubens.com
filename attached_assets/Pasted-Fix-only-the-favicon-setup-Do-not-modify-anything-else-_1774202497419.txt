Fix only the favicon setup.

Do not modify anything else:
- do not touch layout structure
- do not touch responsive behavior
- do not touch navigation
- do not touch footer
- do not touch legal or sitemap pages
- do not touch CSS except favicon-related references if needed

Goal:
Use the correct favicon hierarchy so browsers stop using the wrong icon.

-----------------------------------
1. MAIN FAVICON RULE
-----------------------------------

The main favicon must be:
- /favicon.ico

This must be the primary favicon used by the site.

Do NOT use:
- /icons/icon.svg
as the primary favicon anymore.

-----------------------------------
2. LAYOUT METADATA
-----------------------------------

In src/app/layout.tsx, update the icons metadata so the primary favicon is:

icons: {
  icon: "/favicon.ico",
  shortcut: "/favicon.ico"
}

If there are any existing references to:
- /icons/icon.svg
- /icons/icon-v2.svg
- /favicon-v2.ico
or any other favicon path used as primary,
remove them from the primary favicon setup.

-----------------------------------
3. SVG ICON
-----------------------------------

Do not delete /icons/icon.svg.
Keep it in the project if it is used elsewhere.

But:
- it must NOT be used as the main favicon
- it must NOT replace favicon.ico in metadata.icons primary setup

-----------------------------------
4. FILE VERIFICATION
-----------------------------------

Verify that:
- /public/favicon.ico exists
- it contains the correct final icon

If multiple favicon files or conflicting references exist,
keep only the correct primary favicon behavior:
- browsers should request /favicon.ico as the main favicon

-----------------------------------
5. REMOVE CONFLICTS
-----------------------------------

Check for any other favicon declarations and remove conflicts.

This includes:
- manual head tags
- duplicate icon declarations
- references that make browsers load /icons/icon.svg as favicon
- any old or conflicting favicon path

Goal:
Only the correct favicon.ico should be used as main favicon.

-----------------------------------
6. VERIFICATION
-----------------------------------

After the fix, verify:
- the site uses /favicon.ico as the main favicon
- /icons/icon.svg is no longer used as primary favicon
- there are no conflicting favicon declarations

-----------------------------------
OUTPUT
-----------------------------------

At the end, tell me only:
- what favicon references were removed
- what the final metadata.icons configuration is
- confirm that /favicon.ico is now the primary favicon
- confirm that /icons/icon.svg is no longer used as the main favicon